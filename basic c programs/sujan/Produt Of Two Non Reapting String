#include <stdio.h>
#include <string.h>

int main() {
    char str1[] = "sujxx";
    char str2[] = "plane";

    char *p = #include<stdio.h>
#include<string.h>
#include<ctype.h>
int main ()
{
    char s[1000][1000];
    int  s1[1000];
    int i,j,k,num,n,n1,count=0,count1=0,num2;
    int printed[1000]={0};
    int temp,temp1;
    
    int max=0,max2=0;
    scanf("%d",&n);
    
    for(i=0;i<n;i++)
    {
        scanf(" %s",s[i]);
    }
    
    for(i=0;i<n;i++)
    {
        for(j=i+1;j<n;j++)
        {
         char*token=strpbrk(s[i],s[j]);
         if(token==NULL)
         {
        
            num=strlen(s[i]);
            num2=strlen(s[j]);
            
            if(num>max)
            {
                max=num;
                temp=i;
                
            }
            if(num2>max2)
            max2=num2;
            temp1=j;
         
        } }
    }
    printf("%s",s[temp]);
    printf("%s",s[temp1]);
}(str1, str2);

    if (p)
        printf("First common character: %c\n", *p);
    else
        printf("No common character found.%c\n",*p);

    return 0;
}
